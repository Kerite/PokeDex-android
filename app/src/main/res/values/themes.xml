<resources>
    <!-- ======================= -->
    <!-- Base application theme. -->
    <!-- ======================= -->
    <style name="Base.Theme.PokemonDex" parent="Theme.Material3.DynamicColors.DayNight">
        <item name="android:windowLightStatusBar">?attr/isLightTheme</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="bottomNavigationStyle">@style/Widget.Pokedex.BottomNavigationView</item>
        <item name="actionModeCloseButtonStyle">@style/Theme.Pokedex.ActionMode.CloseButton</item>
        <item name="android:windowActionBar">false</item>
        <item name="android:windowNoTitle">true</item>
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="preferenceTheme">@style/PreferenceThemeOverlay.Rikka.Material3</item>
    </style>

    <!--Default Theme-->
    <style name="Theme.PokemonDex" parent="Base.Theme.PokemonDex">
        <!-- Transparent system bars for edge-to-edge. -->
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:windowLightStatusBar">?attr/isLightTheme</item>
    </style>

    <style name="SelectableTextViewText" parent="Widget.AppCompat.TextView">
        <item name="android:textAppearance">@style/TextAppearance.AppCompat.Subhead</item>
        <item name="android:gravity">center</item>
        <item name="android:textStyle">bold</item>
    </style>
</resources>